{
  "name": "@dbz-code/memory",
  "version": "0.0.0",
  "private": false,
  "description": "Memory subsystem with local-first SQLite + sqlite-vec storage",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --project tsconfig.build.json --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "devDependencies": {
    "rimraf": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
