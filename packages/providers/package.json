{
  "name": "@dbz-code/providers",
  "version": "0.1.0",
  "private": false,
  "description": "Provider System — model provider adapters for DBZ Code",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "dev": "tsc --project tsconfig.build.json --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "dependencies": {
    "@dbz-code/events": "workspace:*",
    "@dbz-code/shared": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "*",
    "typescript": "*",
    "vitest": "*"
  }
}
